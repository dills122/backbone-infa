# Backbone environment defaults: populate before running docker compose (see README service onboarding).

# Caddy / global settings
CADDY_ADMIN_EMAIL=you@dsteele.dev

# Umami database credentials
UMAMI_DB_PASS=
UMAMI_APP_SECRET=

# Optional defaults
TZ=America/New_York
DOMAIN_BASE=dsteele.dev
